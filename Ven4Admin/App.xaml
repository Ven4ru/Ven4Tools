<Application x:Class="Ven4Admin.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BgMain"   Color="#1A1A2E"/>
        <SolidColorBrush x:Key="BgCard"   Color="#252540"/>
        <SolidColorBrush x:Key="BgBorder" Color="#3A3A5C"/>
        <SolidColorBrush x:Key="Accent"   Color="#7C72F6"/>
        <SolidColorBrush x:Key="TextMain" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextSec"  Color="#888888"/>
        <SolidColorBrush x:Key="Green"    Color="#4CAF50"/>
        <SolidColorBrush x:Key="Orange"   Color="#FF9800"/>
        <SolidColorBrush x:Key="Red"      Color="#F44336"/>

        <Style TargetType="Button">
            <Setter Property="Background"       Value="#2E2E50"/>
            <Setter Property="Foreground"       Value="#E0E0E0"/>
            <Setter Property="BorderBrush"      Value="#4A4A6C"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8,4"/>
            <Setter Property="Margin"           Value="0,2"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E6C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5C52C6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background"      Value="#252540"/>
            <Setter Property="Foreground"      Value="#E0E0E0"/>
            <Setter Property="BorderBrush"     Value="#3A3A5C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="4,3"/>
            <Setter Property="CaretBrush"      Value="#E0E0E0"/>
            <Setter Property="SelectionBrush"  Value="#7C72F6"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background"      Value="#252540"/>
            <Setter Property="Foreground"      Value="#E0E0E0"/>
            <Setter Property="BorderBrush"     Value="#3A3A5C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="4,3"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background"      Value="#252540"/>
            <Setter Property="Foreground"      Value="#E0E0E0"/>
            <Setter Property="BorderBrush"     Value="#3A3A5C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground"  Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3A3A5C"/>
            <Setter Property="Margin"      Value="0,0,0,6"/>
            <Setter Property="Padding"     Value="6,4"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="#7C72F6"
                                   FontWeight="SemiBold" FontSize="11"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground"  Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3A3A5C"/>
            <Setter Property="Margin"      Value="0,0,0,6"/>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#3A3A5C"/>
            <Setter Property="Foreground" Value="#7C72F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="6"/>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#1A1A2E"/>
        </Style>
    </Application.Resources>
</Application>
