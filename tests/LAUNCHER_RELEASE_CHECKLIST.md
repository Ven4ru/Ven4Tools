# Проверка Ven4Tools Launcher перед релизом

Версия: `____________`  
Дата: `____________`  
Тестировщик: `____________`

Релиз запрещён, пока каждый применимый пункт не отмечен и не приложен результат автоматических тестов.

## Автоматические проверки

- [ ] Release-сборка всего `Ven4Tools.sln` — 0 ошибок и 0 предупреждений.
- [ ] Все unit/property-тесты прошли.
- [ ] Coverage критического сервисного слоя не ниже установленного gate.
- [ ] UI smoke/snapshot прошёл два раза подряд.
- [ ] NuGet audit не обнаружил известных уязвимостей.
- [ ] Gitleaks/проверка секретов прошла.

## Чистый запуск

- [ ] Launcher запускается после чистой установки.
- [ ] Главное окно появляется без лишних уведомлений и блокирующих диалогов.
- [ ] Повторный запуск корректно активирует/отклоняет второй экземпляр.
- [ ] Запуск без сети не блокирует основной интерфейс.

## Кнопки главного окна

- [ ] «Выбрать папку»: выбор, отмена диалога и сохранение пути.
- [ ] «Найти Ven4Tools на диске»: найден один, найдено несколько, ничего не найдено.
- [ ] «Проверить обновления»: актуальная версия, новая версия, сеть недоступна.
- [ ] «Загрузить Ven4Tools»: успешная загрузка и установка.
- [ ] «Отмена»: отмена загрузки и отсутствие `.partial`/повреждённого клиента.
- [ ] «Запустить Ven4Tools»: клиент запускается из выбранного каталога.
- [ ] «История изменений»: панель открывается, закрывается и не ломает layout.
- [ ] «Удалить клиент»: отмена подтверждения и удаление только sandbox-клиента.
- [ ] «Выход»: процесс и tray icon завершаются.

## Настройки и состояния

- [ ] Фоновые обновления включаются/выключаются и сохраняются.
- [ ] Запуск свёрнутым в трей включается/выключается и сохраняется.
- [ ] Автозапуск включается/выключается только в тестовом профиле.
- [ ] Список версий открывается, выбранная версия применяется.
- [ ] Кнопка обновления лаунчера проверена при доступном обновлении.
- [ ] Кнопка установки компонентов проверена при отсутствующем компоненте.
- [ ] Кнопка закрытия details-панели работает.
- [ ] Сворачивание, восстановление из трея и двойной клик по tray icon работают.

## Ошибки и восстановление

- [ ] Основной источник недоступен — выполняется тихий fallback.
- [ ] Повреждённый файл основного источника — используется проверенный fallback.
- [ ] Оба источника недоступны — текущий клиент остаётся рабочим.
- [ ] ZIP повреждён — установка прекращается без изменения клиента.
- [ ] ZIP path traversal/symlink — архив отклоняется.
- [ ] Отмена во время распаковки/commit восстанавливает предыдущую версию.
- [ ] Ошибка commit восстанавливает предыдущую версию.
- [ ] Повторная попытка после ошибки работает.

## Setup и обновление

- [ ] Setup устанавливает launcher без UAC в ожидаемый каталог.
- [ ] Повторная установка поверх предыдущей версии проходит корректно.
- [ ] Версия exe, Setup и записи «Программы и компоненты» совпадает.
- [ ] Ярлыки рабочего стола и меню «Пуск» работают.
- [ ] Self-update заменяет launcher и повторно запускает его.
- [ ] Деинсталлятор удаляет только файлы launcher и созданные им ярлыки.
- [ ] После установки через Setup полный smoke всех основных кнопок повторён.

## Конфиденциальность и UX

- [ ] В логах, snapshot и release assets нет локальных путей, токенов и личных данных.
- [ ] Защитные проверки не добавили уведомлений при штатном запуске.
- [ ] Нет заметной дополнительной задержки открытия главного окна.
- [ ] Все сообщения пользователю понятны и появляются только при необходимом действии.

Итог: `PASS / BLOCKED`  
Причина блокировки: `____________________________________________________________`

---

## Результат автоматизированного прохода 2.1.0 — 01.07.2026

- Release-сборка: PASS, 0 ошибок и 0 предупреждений.
- Unit/property-тесты: PASS, 63 из 63.
- Coverage критического слоя: PASS, 92% строк и 90,24% ветвей при gate 80%.
- UI smoke/snapshot: PASS два раза подряд; отдельно PASS для опубликованного и установленного EXE.
- NuGet audit: PASS, известных уязвимостей нет.
- Gitleaks: PASS, утечек не найдено.
- Сборка Setup, тихая установка, повторная установка и проверка версии/ярлыков/реестра: PASS.
- Тихая деинсталляция с сохранением пользовательских данных и последующая переустановка: PASS.

Итог автоматизированного прохода: `PASS`  
Ручной проход полного списка сценариев: `BLOCKED`  
Причина блокировки: обязательный Windows UI automation plugin установлен без
`scripts/computer-use-client.mjs`; пункты, не покрытые FlaUI/unit/integration-тестами,
не отмечены как пройденные.

---

## Результат ручного UI-прохода 2.1.0 — 01.07.2026

Среда: установленный `Ven4Tools.Launcher.exe` версии `2.1.0`
(`ProductVersion 2.1.0+6ce10d2...`), Windows 11 build 26200, Computer Use.

Проверено вручную:

- PASS: чистое открытие главного окна без блокирующих уведомлений.
- PASS: повторный запуск отклоняется сообщением «Ven4Tools Launcher уже запущен».
- PASS: диалог выбора папки открывается; отмена сохраняет исходный путь.
- PASS: поиск клиента корректно показывает ветку «не найдено».
- PASS: проверка обновлений сообщает, что установлена последняя версия лаунчера.
- PASS: панель истории изменений открывается и закрывается без нарушения layout.
- PASS: настройки фоновых обновлений и запуска свёрнутым сохраняются; после проверки
  восстановлены исходные значения.
- PASS: список версий открывается, выбор `3.4.12` применяется, затем восстановлен
  исходный выбор `3.4.13`.
- PASS: клиент запускается из выбранного каталога и открывает главное окно.
- PASS: кнопка «Выход» завершает процесс лаунчера.

Не завершено вручную:

- выбор и сохранение нового sandbox-пути;
- ветки поиска с одним и несколькими найденными клиентами;
- загрузка, отмена загрузки и установка в sandbox;
- подтверждённое удаление только sandbox-клиента;
- offline/fallback/corrupt ZIP/rollback-сценарии;
- autostart, tray icon, восстановление двойным кликом и self-update;
- установка компонентов и доступное обновление лаунчера.

Во время проверки отмены удаления координатный клик Computer Use попал в
подтверждение удаления реального клиента. Лаунчер сообщил об отказе удаления
занятого `clrjit.dll`, сохранил настройки и логи и завершил операцию; клиент перед
этим был успешно запущен. Дальнейшие разрушительные сценарии остановлены.

Итог ручного UI-прохода: `BLOCKED`

Причина блокировки: полный ручной проход требует изолированного sandbox-клиента и
управляемых сетевых фикстур; установленный EXE также содержит build metadata
`6ce10d2`, а не проверяемый коммит `8657f18`.

## План следующего ручного прохода

1. Включить Windows Sandbox (`Containers-DisposableClientVM`) и перезагрузить ПК.
2. Собрать Setup и portable EXE из одного проверяемого commit SHA.
3. Подключить каталог артефактов в `.wsb` только для чтения и включить сеть.
4. Внутри sandbox установить launcher и выбрать
   `C:\SandboxData\Ven4Tools_Client` как каталог клиента.
5. Пройти загрузку, отмену, установку, удаление, ярлыки, autostart, tray,
   offline/fallback, повреждённый ZIP, rollback и self-update.
6. Сверить версии EXE, Setup, записи uninstall и commit metadata.
7. Обновить этот чеклист до `PASS` и только после этого запустить
   `.github/workflows/launcher-release.yml` с `RELEASE-APPROVED`.

---

## Продолжение ручного UI-прохода в Windows Sandbox — 02.07.2026

Среда: Windows Sandbox с сетью; каталог
`C:\Users\Vench\Ven4Tools\_release\gate-2a057fd` подключён как
`C:\GateArtifacts` только для чтения.

Проверяемые артефакты:

- `Ven4Tools.Setup-2.1.0.exe`, SHA-256
  `4937527A27D084D25887627152068E89BD94021A5975B393549219FCC7B5B2D6`;
- `Ven4Tools.Launcher-2.1.0.exe`, `ProductVersion
  2.1.0+2a057fd5e849ed26f72b89c076c9d460ffa2491c`, SHA-256
  `A979B12ABCF8675CBD5ADC61D14E498A3A3F263C162ACAD176573705DBD8B2D5`;
- клиент `3.4.13`, собранный из того же commit SHA `2a057fd`.

Проверено:

- PASS: Setup 2.1.0 устанавливается в чистой Windows Sandbox и создаёт ярлык.
- PASS: установленный launcher открывается без блокирующего диалога.
- PASS: launcher определяет Windows 10 build 26100 и отсутствие WebView2 Runtime,
  Visual C++ Redistributable и winget.
- PASS: launcher сообщает, что версия launcher актуальна.
- PASS: выбор версии клиента по умолчанию установлен на `3.4.13`.
- PASS: новый sandbox-путь
  `C:\Users\WDAGUtilityAccount\Downloads\Ven4Tools_Client` сохраняется.
- PASS: клиент 3.4.13 скачивается; SHA-256 подтверждён; архив безопасно
  распаковывается.
- PASS: после устранения неполадок с отсутствующим winget клиент запускается и
  открывает главное окно.

Примечание: установка winget через «Устранить неполадки» не влияет на PASS
проверки запуска клиента, но изменяет чистую среду и учитывается отдельно при
проверке установки компонентов.

Проверка удаления:

- PASS: файлы скачанного sandbox-клиента удалены.
- PASS: пустой корневой каталог клиента оставлен намеренно для консистентного
  состояния launcher.
- FAIL: вместе с ярлыками клиента удалён ярлык установленного launcher
  `Ven4Tools Launcher.lnk` с рабочего стола.

Причина FAIL подтверждена в `MainWindow.Download.cs`: обработчик удаления клиента
без проверки назначения удаляет с рабочего стола имена `Ven4Tools.lnk`,
`Ven4Tools Launcher.lnk` и `Ven4Tools Client.lnk`, а также удаляет каталог
`Ven4Tools` в меню «Пуск». Удаление клиента не изолировано от ярлыков launcher.

Исправление подготовлено локально 02.07.2026:

- удаляются только `Ven4Tools.lnk` и `Ven4Tools Client.lnk`;
- ярлык `Ven4Tools Launcher.lnk`, ярлык деинсталлятора и непустая папка launcher
  в меню «Пуск» сохраняются;
- запись автозапуска `Ven4Tools.Launcher` больше не удаляется;
- регрессионные тесты: PASS, 65 из 65;
- Release-сборка с `-warnaserror`: PASS, 0 ошибок и 0 предупреждений.

Статус исправления: `AWAITING SANDBOX RETEST`.

Итог текущего этапа: `PASS` для чистой установки launcher, загрузки, проверки
целостности, распаковки и запуска клиента. Сценарий удаления клиента: `FAIL`.
Полный ручной release gate остаётся `BLOCKED`.

## Решение владельца проекта — 02.07.2026

Владелец проекта явно разрешил выпуск launcher 2.1.0 и клиента 3.5.0 после
исправления дефекта удаления ярлыка. Непройденный повторный sandbox-сценарий
зафиксирован выше и принят владельцем как остаточный риск. Это решение разрешает
передать workflow значение `RELEASE-APPROVED`, но не меняет исторический результат
незавершённого ручного прохода на технический `PASS`.
