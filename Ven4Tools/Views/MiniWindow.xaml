<Window x:Class="Ven4Tools.Views.MiniWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ven4Tools Mini" Height="380" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="Manual"
        Topmost="True"
        Background="{DynamicResource WindowBackground}"
        Closing="Window_Closing"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style x:Key="MiniBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="⚡" FontSize="18" Margin="0,0,6,0"
                           VerticalAlignment="Center"/>
                <TextBlock Text="Быстрая установка" FontSize="15" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimary}"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <Button x:Name="btnClose" Content="✕" Width="28" Height="28"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextSecondary}"
                    FontSize="12" Cursor="Hand"
                    Click="BtnClose_Click"/>
        </Grid>

        <!-- Search box -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackground}"
                CornerRadius="8" Padding="10,0" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center"
                           Margin="0,0,8,0" Foreground="{DynamicResource TextSecondary}"/>
                <TextBox x:Name="txtMiniSearch" Grid.Column="1" Height="38"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource TextPrimary}"
                         VerticalContentAlignment="Center" FontSize="14"
                         Tag="Введите название..."
                         TextChanged="TxtMiniSearch_TextChanged"
                         KeyDown="TxtMiniSearch_KeyDown"/>
            </Grid>
        </Border>

        <!-- Results -->
        <ListBox x:Name="lstMiniResults" Grid.Row="2"
                 Background="{DynamicResource CardBackground}"
                 BorderThickness="0"
                 SelectionMode="Single"
                 MouseDoubleClick="LstMiniResults_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       Foreground="{DynamicResource TextPrimary}"
                                       FontSize="13" FontWeight="Medium"/>
                            <TextBlock Text="{Binding Category}"
                                       Foreground="{DynamicResource TextSecondary}"
                                       FontSize="10"/>
                        </StackPanel>
                        <Border Grid.Column="1" CornerRadius="4" Padding="6,2"
                                Background="{DynamicResource BorderBrush}"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding WingetId}"
                                       FontSize="9" Foreground="{DynamicResource TextSecondary}"
                                       MaxWidth="100" TextTrimming="CharacterEllipsis"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="4,2"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Status / progress -->
        <StackPanel Grid.Row="3" Margin="0,8,0,0">
            <ProgressBar x:Name="progressMini" Height="4" Minimum="0" Maximum="100"
                         Foreground="{DynamicResource AccentColor}"
                         Background="{DynamicResource BorderBrush}"
                         Visibility="Collapsed"/>
            <TextBlock x:Name="txtMiniStatus" Text="Введите запрос для поиска"
                       Foreground="{DynamicResource TextSecondary}"
                       FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnMiniInstall" Style="{StaticResource MiniBtn}"
                    Content="🚀 Установить" FontWeight="SemiBold"
                    Background="{DynamicResource AccentColor}" Foreground="White"
                    IsEnabled="False" Click="BtnMiniInstall_Click"/>
            <Button x:Name="btnMiniOpenFull" Grid.Column="2" Style="{StaticResource MiniBtn}"
                    Content="⛶ Открыть полный" Width="140"
                    Click="BtnMiniOpenFull_Click"/>
        </Grid>
    </Grid>
</Window>
