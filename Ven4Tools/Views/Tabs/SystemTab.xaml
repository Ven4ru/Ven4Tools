<UserControl x:Class="Ven4Tools.Views.Tabs.SystemTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ContentBackground}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Системные настройки" FontSize="24" FontWeight="Bold" 
                       Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20"/>
            
            <!-- Автозагрузка -->
            <GroupBox Header="🚀 Автозагрузка" Margin="0,0,0,15">
                <StackPanel>
                    <CheckBox x:Name="chkAutoStart" Content="Запускать Ven4Tools при старте Windows" 
                              Margin="10" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Добавляет программу в автозагрузку системы" 
                               Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="10,0,10,10"/>
                </StackPanel>
            </GroupBox>
            
            <!-- Уведомления -->
            <GroupBox Header="🔔 Уведомления" Margin="0,0,0,15">
                <StackPanel>
                    <CheckBox x:Name="chkNotifications" Content="Показывать уведомления о завершении установки" 
                              Margin="10" Foreground="{DynamicResource TextPrimary}" IsChecked="True"/>
                    <CheckBox x:Name="chkUpdateNotifications" Content="Проверять обновления при запуске" 
                              Margin="10" Foreground="{DynamicResource TextPrimary}" IsChecked="True"/>
                </StackPanel>
            </GroupBox>
            
            <!-- Таймауты -->
            <GroupBox Header="⏱️ Таймауты" Margin="0,0,0,15">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Таймаут загрузки каталога:" Grid.Row="0" Grid.Column="0" 
                               Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                    <Slider x:Name="sliderCatalogTimeout" Grid.Row="0" Grid.Column="1" 
                            Minimum="3" Maximum="30" Value="10" Margin="10,0" 
                            Foreground="{DynamicResource AccentColor}"/>
                    <TextBlock x:Name="txtCatalogTimeout" Grid.Row="0" Grid.Column="2" 
                               Text="10 сек" Foreground="{DynamicResource TextSecondary}"/>
                    
                    <TextBlock Text="Таймаут проверки доступности:" Grid.Row="1" Grid.Column="0" 
                               Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" Margin="0,10,0,0"/>
                    <Slider x:Name="sliderCheckTimeout" Grid.Row="1" Grid.Column="1" 
                            Minimum="5" Maximum="60" Value="15" Margin="10,10,10,0" 
                            Foreground="{DynamicResource AccentColor}"/>
                    <TextBlock x:Name="txtCheckTimeout" Grid.Row="1" Grid.Column="2" 
                               Text="15 сек" Foreground="{DynamicResource TextSecondary}" Margin="0,10,0,0"/>
                </Grid>
            </GroupBox>
            
            <!-- Информация о системе -->
            <GroupBox Header="💻 Информация о системе" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ОС:" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock x:Name="txtOSVersion" Grid.Column="1" Foreground="{DynamicResource TextPrimary}"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Процессор:" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock x:Name="txtProcessor" Grid.Column="1" Foreground="{DynamicResource TextPrimary}"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ОЗУ:" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock x:Name="txtRAM" Grid.Column="1" Foreground="{DynamicResource TextPrimary}"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Версия Ven4Tools:" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock x:Name="txtAppVersion" Grid.Column="1" Foreground="{DynamicResource TextPrimary}"/>
                    </Grid>
                    <Button x:Name="btnCopySystemInfo" Content="📋 Копировать информацию" 
                            Height="30" Width="180" Margin="0,15,0,0" HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>
            
            <!-- Логи -->
            <GroupBox Header="📋 Логи" Margin="0,0,0,15">
                <StackPanel>
                    <Button x:Name="btnOpenLogs" Content="📁 Открыть папку с логами" 
                            Height="35" Margin="10" HorizontalAlignment="Left" Width="200"/>
                    <Button x:Name="btnClearLogs" Content="🗑️ Очистить логи" 
                            Height="35" Margin="10,0,10,10" HorizontalAlignment="Left" Width="150"/>
                </StackPanel>
            </GroupBox>
            
            <!-- Turbo Boost -->
            <GroupBox Header="⚡ Turbo Boost (Intel)" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Управление технологией Intel Turbo Boost" 
                               Foreground="{DynamicResource TextSecondary}" Margin="10,5,10,0"/>
                    <StackPanel Orientation="Horizontal" Margin="10,10">
                        <Button x:Name="btnDisableTurboBoost" Content="❌ Отключить Turbo Boost" 
                                Height="35" Width="180" Margin="0,0,10,0"/>
                        <Button x:Name="btnEnableTurboBoost" Content="✅ Включить Turbo Boost" 
                                Height="35" Width="180"/>
                    </StackPanel>
                    <TextBlock Text="⚠️ Требуется перезагрузка для применения изменений" 
                               Foreground="#FFA500" FontSize="11" Margin="10,5,10,10"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
