<Window x:Class="Ven4Tools.Views.YandexAuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Войти через Яндекс — Ven4Tools"
        Width="500" Height="660"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <wv2:WebView2 x:Name="webView" Grid.Row="0"/>

        <Border Grid.Row="1" Background="{DynamicResource SidebarBackground}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="txtStatus" Text="Загрузка..."
                       Padding="12,7" FontSize="12"
                       Foreground="{DynamicResource TextSecondary}"/>
        </Border>
    </Grid>
</Window>
